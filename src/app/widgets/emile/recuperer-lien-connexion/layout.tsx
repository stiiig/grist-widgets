import type { Metadata } from "next";
export const metadata: Metadata = { title: "EMILE – Accéder à mon espace" };
export default function RecupererLienConnexionLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
