import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "EMILE – Validation compte orienteur",
};

export default function ValidationCompteLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
