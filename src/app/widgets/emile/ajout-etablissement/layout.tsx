import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "EMILE – Ajout établissement",
};

export default function EtablissementLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
